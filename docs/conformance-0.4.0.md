# Protocol 0.4.0 conformance record

## Scope

Protocol `0.4.0` adds one schema without changing prior versions:

- `profiles/reservation-service-result.schema.json`.

The profile reports four stable behavior groups for the Go reservation-service
task. The TypeScript and Python packages remain private and unpublished.

## Verification

Run the TypeScript and Python commands in the repository `README.md`. Both
language suites validate the example, terminal branches, closed group shape,
generated bindings, and recorded canonical schema digest.

This record does not establish benchmark execution, evaluator correctness,
model performance, or publication.
